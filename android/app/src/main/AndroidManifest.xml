<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Permissões básicas -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- Permissões para biometria -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    
    <!-- Permissões para câmera e galeria -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- Permissões de localização (check-in por geolocalização) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- HTTP da API em dev/LAN; com API só HTTPS, use usesCleartextTraffic false. -->
    <application
        android:label="Intellisys"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules">
        <!-- FCM: usar o canal de alta importância criado pelo Flutter (flutter_local_notifications). -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="imobx_alerts" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/ic_launcher" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- App Links (https). O domínio é o do SPA do sistema
                 (FRONTEND_URL/APP_URL do backend = https://intellisysbr.com/sistema).
                 autoVerify exige https://intellisysbr.com/.well-known/assetlinks.json
                 no ar NO MOMENTO DA INSTALAÇÃO (ver deploy/well-known/README.md).

                 Os pathPrefix são a WHITELIST: só entram caminhos que o
                 AppDeepLink sabe traduzir. Android não tem exclusão, então
                 /sistema INTEIRO nunca é declarado — senão sequestraríamos
                 /sistema/reset-password/{token} e /sistema/verify-email, que
                 PRECISAM abrir no navegador. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="intellisysbr.com"/>
                <data android:pathPrefix="/sistema/kanban"/>
                <data android:pathPrefix="/sistema/tasks/"/>
                <data android:pathPrefix="/sistema/calendar"/>
                <data android:pathPrefix="/sistema/appointments"/>
                <data android:pathPrefix="/sistema/properties"/>
                <data android:pathPrefix="/sistema/clients"/>
                <data android:pathPrefix="/sistema/proposals"/>
                <data android:pathPrefix="/sistema/fichas-proposta"/>
                <data android:pathPrefix="/sistema/fichas-venda"/>
                <data android:pathPrefix="/sistema/inspection"/>
                <data android:pathPrefix="/sistema/documents"/>
                <data android:pathPrefix="/sistema/checklists"/>
                <data android:pathPrefix="/sistema/check-in"/>
                <data android:pathPrefix="/sistema/dashboard"/>
                <data android:pathPrefix="/sistema/chat"/>
                <data android:pathPrefix="/sistema/integrations"/>
            </intent-filter>
            <!-- Esquema próprio dreamkeys:// (Ilha Dinâmica do check-in). No
                 iOS já era capturado pelo AppDelegate; no Android faltava este
                 filtro — sem ele o intent nunca chegava na MainActivity.
                 Sem autoVerify: scheme custom não passa por verificação. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="dreamkeys"/>
            </intent-filter>
        </activity>
        <!-- Activity do image_cropper -->
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Queries para url_launcher -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
    </queries>
</manifest>
