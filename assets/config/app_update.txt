# Fallback de ÚLTIMA instância do aviso de atualização (produção).
# A fonte primária é o iTunes Lookup (versão real na App Store) e o
# fallback intermediário é GET /app/mobile-version no backend — este
# arquivo só vale se os dois falharem.
# Linha 1: versão marketing (ex. 2.1.1)
# Linha 2: build number (0 = ignora build no desempate)
# Linha 3: URL da página do app na App Store (OBRIGATÓRIA — sem ela o
#          fallback é ignorado; NUNCA usar link de TestFlight aqui)
